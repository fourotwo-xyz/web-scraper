# ── Scripts ──────────────────────────────────────────────
BASE_URL=http://localhost:8080

# ── Server ──────────────────────────────────────────────
PORT=8080

# ── TLS ──────────────────────────────────────────────

# Your domain name (required for TLS)
DOMAIN=

# Port your application listens on
APP_PORT=8080

# Enable Caddy debug logs
ENABLE_CADDY_LOGS=true

# Use Let's Encrypt staging environment (for testing)
# Set to true to avoid rate limits during development
ACME_STAGING=false

# Force certificate reissue even if a valid one exists
# Useful when you need to update SANs or force a renewal
ACME_FORCE_ISSUE=false


# ── x402 Payment ─────────────────────────────────────────
# Wallet that receives USDC payments on Base
PAYMENT_RECEIVER_ADDRESS=0xYourWalletAddressHere

# Agent wallet for ERC-8004 feedback auth. When set, POST /scrape responses
# include feedbackAuth (agentId, taskId, signature) so clients can submit feedback.
# Use the same identity as your agent’s on-chain 8004 registration if applicable.
AGENT_PRIVATE_KEY=

# Facilitator URL (default: testnet — x402.org)
# Mainnet examples:
#   https://api.developer.coinbase.com/x402/facilitator
#   https://facilitator.openx402.ai
#   https://facilitator.infra402.com
X402_FACILITATOR_URL=https://x402.org/facilitator

# CAIP-2 network: eip155:84532 = Base Sepolia, eip155:8453 = Base Mainnet
X402_NETWORK=eip155:84532

# ── Simplescraper (required for POST /scrape) ─────────────
# https://simplescraper.io/docs/api-guide
SIMPLESCRAPER_API_KEY=

# ── Agent card (scripts/generate-agent-card.ts) ───────────
# Required: payment receiver and base URL (no sensible defaults)
# PAYMENT_RECEIVER_ADDRESS and BASE_URL are required above.

# Required for --upload: Pinata JWT to pin agent card to IPFS
# https://app.pinata.cloud/
PINATA_JWT=

# Required for --register (generate-agent-card): wallet that signs the tx and owns the Agent ID NFT
IDENTITY_SIGNER_PRIVATE_KEY=

# Required for boost-reputation: wallet that signs feedback on the Reputation Registry
REPUTATION_SIGNER_PRIVATE_KEY=

# Base RPC URL (testnet: Base Sepolia; mainnet: https://mainnet.base.org)
BASE_RPC_URL=https://sepolia.base.org

# ERC-8004 Identity Registry (register, agent IDs). Testnet: 0x8004A818BFB912233c491871b3d84c89A494BD9e; Mainnet: 0x8004A169FB4a3325136EB29fA0ceB6D2e539a432
IDENTITY_REGISTRY_ADDRESS=0x8004A818BFB912233c491871b3d84c89A494BD9e

# ERC-8004 Reputation Registry (giveFeedback). Different contract from Identity. Testnet: 0x8004B663056A597Dffe9eCcC1965A193B7388713; Mainnet: 0x8004BAa17C55a88189AE136b182e5fdA19dE9b63
REPUTATION_REGISTRY_ADDRESS=0x8004B663056A597Dffe9eCcC1965A193B7388713

# Your agent's Identity Registry token ID (uint256). Required for scripts/boost-reputation.ts.
AGENT_ID=
